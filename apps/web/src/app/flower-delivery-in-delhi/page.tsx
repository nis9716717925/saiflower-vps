import { StubPage } from '@/components/StubPage';

export default function DelhiDeliveryPage() {
  return (
    <StubPage
      title="Flower Delivery in Delhi"
      description="SEO city landing — full dynamic_pages / collection content will be served from Postgres in Phase 7."
    />
  );
}