# @saiflower/shared

Shared TypeScript types, URL helpers, and constants used by `apps/web` and `apps/server`.

## Rules

- URL helpers must mirror production `.htaccess` and PHP `url_helper.php`.
- `CHECKOUT_MODE` stays `whatsapp_confirm` until product explicitly changes it.
- Do not introduce UI or design tokens here.
